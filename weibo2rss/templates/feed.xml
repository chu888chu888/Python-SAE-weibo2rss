<?xml version="1.0" encoding="utf-8" ?>

<rss version="2.0">

<channel>

    <title>{{ user.name }}的微博</title>

    <link>http://weibo.com/u/{{ user.id }}</link>

    <description>由weibo2rss生成的feed</description>

    <language>zh-cn</language>

    <copyright></copyright> 

    <pubDate>{{ datenow }}</pubDate>

    {% for tweet in statuses %}

        <item>
    
            <title><![CDATA[{{ tweet.text }}]]></title>
    
            <link>http://api.t.sina.com.cn/{{ tweet.user.id }}/statuses/{{ tweet.id }}?source={{ appkey }}</link>
    
            <author>{{ tweet.user.name }}</author>
    
            <pubDate>{{ tweet.created_at }}</pubDate>
    
            <description>
                <![CDATA[
                    <p>{{ tweet.text }}</p>
                    {% if tweet.original_pic %}
                        <img src="{{ tweet.original_pic }}" alt="" style="border:0px">
                    {% endif %}
                ]]>
            </description>
    
            <category></category>
    
        </item>

    {% endfor %}

</channel>

</rss>